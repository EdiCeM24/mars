# Exported from Render on 2026-03-02T10:18:30Z
version: "1"
databases:
- name: marsdb
  databaseName: marsdb_w6iy
  user: marsdb_w6iy_user
  plan: free
  region: oregon
  ipAllowList:
  - source: 0.0.0.0/0
    description: everywhere
  postgresMajorVersion: "18"
services:
  - type: web
    runtime: python
    name: mars
    buildCommand: "./build.sh"
    startCommand: "gunicorn mars.wsgi:application"
    envVars:
      - key: SECRET_KEY
        sync: false # Value must be set in the Render Dashboard for security
      - key: DEBUG
        value: 'False'
      - key: DATABASE_URL
        fromDatabase:
          name: marsdb
          property: connectionString